apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tms-workerService.fullname" . }}-test-connection"
  labels:
    {{- include "tms-workerService.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tms-workerService.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
