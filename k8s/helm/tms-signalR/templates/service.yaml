apiVersion: v1
kind: Service
metadata:
  name: {{ include "tms-signalR.fullname" . }}
  labels:
    {{- include "tms-signalR.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tms-signalR.selectorLabels" . | nindent 4 }}
