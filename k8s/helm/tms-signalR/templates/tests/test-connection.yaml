apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tms-signalR.fullname" . }}-test-connection"
  labels:
    {{- include "tms-signalR.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tms-signalR.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
