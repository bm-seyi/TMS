apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tms-api.fullname" . }}-test-connection"
  labels:
    {{- include "tms-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tms-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
